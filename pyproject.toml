[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yourcoverage"
version = "0.2.0"
description = "Instagram & website competitor analysis tool for home/décor brands"
requires-python = ">=3.10"
dependencies = [
    "instaloader>=4.10",
    "pyyaml>=6.0",
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
yourcoverage = "yourcoverage.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
